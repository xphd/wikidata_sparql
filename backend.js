 
// Async / await usage
// const jsonArray=await csv().fromFile(csvFilePath);

// add new column and fill in the content

// output new csv, may delete the original cvs for saving space.
// const csv=require('csvtojson')
// csv({output:"line"})
// .fromString(csvStr)
// .subscribe((csvLine)=>{ 
//     // csvLine =>  "1,2,3" and "4,5,6"
// })
//


// query wikidata
